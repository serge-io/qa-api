# API-сервис для вопросов и ответов

Простой сервис на **FastAPI** для управления вопросами и ответами.
Стек: **FastAPI**, **PostgreSQL**, **Docker**, **Docker Compose**,
**uv** (CLI для Python).

------------------------------------------------------------------------

## 📌 Функционал

Сервис предоставляет CRUD-операции для двух сущностей:

### Question (Вопрос)

-   `id: int`
-   `text: str` --- текст вопроса
-   `created_at: datetime`

### Answer (Ответ)

-   `id: int`
-   `question_id: int` --- ссылка на Question
-   `user_id: str` --- идентификатор пользователя (например, UUID)
-   `text: str` --- текст ответа
-   `created_at: datetime`

------------------------------------------------------------------------

## 📡 Методы API

### Вопросы (Questions)

#### Получить список всех вопросов

    GET /questions/

#### Создать новый вопрос

    POST /questions/

Request body:

``` json
{
  "text": "string"
}
```

#### Получить вопрос и все ответы к нему

    GET /questions/{id}

#### Удалить вопрос (вместе с ответами)

    DELETE /questions/{id}

------------------------------------------------------------------------

### Ответы (Answers)

#### Добавить ответ на вопрос

    POST /questions/{id}/answers/

Request body:

``` json
{
  "user_id": "string",
  "text": "string"
}
```

#### Получить конкретный ответ

    GET /answers/{id}

#### Удалить ответ

    DELETE /answers/{id}

------------------------------------------------------------------------

## 🐳 Запуск через Docker

```bash

Собрать и запустить проект

    docker compose up --build

Сервис будет доступен по адресу:

    http://localhost:8000

Документация OpenAPI:

    http://localhost:8000/docs
```
------------------------------------------------------------------------

## ✔️ Тестирование

Тесты запускаются через uv:

    uv run pytest

------------------------------------------------------------------------
